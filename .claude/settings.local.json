{
  "permissions": {
    "allow": [
      "Bash(rm -rf '{configs,src,data')",
      "Bash(mkdir -p data/raw data/processed data/augmented models results logs)",
      "Bash(pip show:*)",
      "Bash(pip3 show:*)",
      "Bash(python3 -m venv .venv)",
      "Bash(.venv/bin/pip install:*)",
      "Bash(.venv/bin/python main.py --stage data)",
      "Bash(.venv/bin/python -c ':*)"
    ]
  }
}
